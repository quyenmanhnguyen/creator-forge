# --- Environment ---
.env
.env.local
.env.*.local

# --- Python ---
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
.eggs/
.venv/
venv/
ENV/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.coverage.*

# --- Node / Electron ---
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.npm/
.cache/
# package-lock.json is not committed — keeps the repo light and matches the
# existing convention. Re-generate locally with `npm install`.
package-lock.json

# Electron build artifacts (these used to be vendored in autogrok-veo3 but
# don't belong in source — electron-builder regenerates them).
desktop/dist-electron/
desktop/release/
desktop/out/
*.dll
*.dylib
*.so
*.pak
*.bin
*.dat
*.exe
*.app
*.ico
LICENSES.chromium.html
LICENSE.electron.txt

# --- Runtime / user data ---
electron-runtime.log
sessions/
output/
debug/
ref-images/
*.log

# --- IDE / OS ---
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
